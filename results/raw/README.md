Raw model responses land here, one JSONL file per model/language/mode.

APPEND-ONLY. Nothing in this repository ever edits or deletes a file in this
directory. Every number in the paper is recomputed from these files, so any
published result can be traced back to the exact model response that produced it.
